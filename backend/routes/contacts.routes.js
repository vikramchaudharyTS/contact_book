import express from 'express' 

const router = express.Router()

router.post('/add', )
router.put('/edit/:id', )
router.get('/search-all', )
router.get('/search-one/:id', )
router.delete('/delete/:id', )


export default router;